export const pagination = {
  // position: ['topRight'],
  showSizeChanger: true,
  showTotal: (total: number) => '共 (0) 条',
  defaultPageSize: 10
};

export const tableSearch = {
  labelWidth: 120
};
